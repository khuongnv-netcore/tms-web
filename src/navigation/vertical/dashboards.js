import { Home, Circle } from 'react-feather'
import { pathKeys, pathNames } from '../../constants'

export default [
  {
    id: 'dashboards',
    title: pathNames.DASHBOARD,
    icon: <Home size={20} />,
    navLink: pathKeys.DASHBOARD,
  }
]
